const fetch = require('node-fetch');

async function testScan() {
  const token = 'YOUR_TEST_TOKEN'; // Actually we need the user's token or we can just query the DB directly to see what happened on Render
  // Wait, we don't have the token.
}
